import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class Master {
  //store data
  //create API calls
  //createsubject and behavour subject
  //utility function, helper function
  addTwoNum(num1: number, num2: number) {
    return num1 + num2;
  }
}
